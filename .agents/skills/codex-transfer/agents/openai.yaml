interface:
  display_name: "Codex Transfer CLI"
  short_description: "Safely inspect, fork, move, archive, and restore Codex sessions"
  default_prompt: "Use $codex-transfer to inspect local Codex sessions and safely perform the requested provider operation."
