# 安全策略

[English](SECURITY.md) | 简体中文

## 支持版本

Codex Relay 目前处于 1.0 之前，安全更新仅支持最新版本。

| 版本 | 是否支持 |
|---|---|
| 最新 Release 与 `main` | 是 |
| 更早版本 | 否 |

存储兼容范围比安全支持范围更窄。即使 Codex Relay 本身仍受支持，也可能拒绝修改未知的未来 Codex schema。

## 威胁模型

Codex Relay 是一个同用户、本地运行的管理工具。它读取 Codex rollout 与 SQLite 索引，并在用户明确确认后写入这些文件。它还会创建包含同类敏感数据的本地备份。

Web 服务只绑定 loopback。本项目不运营云端服务、远程账户系统，也不与启动应用的操作系统用户进行权限隔离。已经能够完全访问该用户文件的进程通常位于信任边界之内。

项目试图保持以下安全属性：

- 不可信标识不能逃逸出选定的 Codex home 或备份根目录。
- 浏览器请求缺少当前进程令牌时不能修改状态。
- Session 选择不能绕过预检、备份、确认和操作后验证。
- 恢复和 Fork 撤销不能静默覆盖后续聊天记录。
- 程序不收集凭据，不把凭据写入 Manifest，也不向浏览器返回凭据。
- 写入前拒绝格式错误或未知的存储。
- app-server 调用不会因 Session 或 Provider 值发生命令注入。

审计链只能检测篡改，不是数字签名。它无法防御能够同时改写应用、审计日志和备份的攻击者。

## 范围内问题

属于报告范围的安全问题包括：

- 路径遍历、符号链接攻击或标识混淆，导致越权读写授权根目录之外的内容。
- 任意文件写入、非预期 SQLite 修改、SQL 注入或命令注入。
- CSRF、跨源请求滥用、令牌泄露、XSS，或其他缺少本地知情确认即可修改状态的路径。
- 恢复或回滚未检测状态分叉，覆盖了新的 Session 历史。
- 备份、Manifest、日志或 API 把凭据或 Session 内容泄露到文档所述本地文件之外。
- 绕过 writer lock、schema、完整性或预检检查并破坏 Codex 状态。
- 审计链验证错误地接受可证明已经修改的事件序列。
- 依赖、安装器或打包流程在没有用户明确意图时执行攻击者控制的代码。

## 范围外问题

除非能与其他问题组合形成信任边界绕过，以下情况通常不在范围内：

- 攻击者已经控制操作系统账户，并能任意修改 Codex 状态和 Codex Relay 备份。
- 用户主动选择恶意的 `--codex-bin`、Codex home 或备份目录。
- Provider 停机、凭据被拒绝、模型不兼容，或无法解密后端生成的 `encrypted_content`。
- 底层 Codex rollout 从未记录逐轮 Provider 来源，因此无法恢复该信息。
- 仅影响用户本人 loopback 实例、且不造成数据损坏或边界逃逸的拒绝服务。
- 没有在受支持版本上证明实际影响的自动扫描结果。
- Codex Relay 已明确拒绝修改的不受支持 Codex schema。

## 报告漏洞

**不要**通过公开 GitHub Issue、Discussion 或 Pull Request 报告安全漏洞。

请使用 [GitHub 私密漏洞报告](https://github.com/XieWeikai/codex-session-manager/security/advisories/new)。如果该入口不可用，请通过仓库所有者 GitHub 个人资料中列出的私密方式联系，并请求安全报告渠道。

报告应包含：

- 受影响的 Codex Relay 版本与操作系统。
- 相关情况下受影响的 Codex 版本或存储 schema。
- 不可信输入来源，以及它到达相关操作的完整路径。
- 使用合成数据的最小复现步骤。
- 预期行为与实际行为。
- 对机密性、完整性或可用性的潜在影响。
- 已知情况下建议的缓解措施。

不要发送真实 API Key、OAuth token、rollout 文件、数据库快照或私人提示词。请遮盖标识，并构造最小化的合成复现。

## 响应目标

以下是尽力而为的目标，不构成服务等级协议：

| 阶段 | 目标时间 |
|---|---|
| 确认收到 | 3 个工作日 |
| 初步评估 | 7 个工作日 |
| 处理期间状态更新 | 至少每 14 天一次 |

修复时间取决于严重程度、可复现性和上游 Codex 行为。项目会与报告者协调披露，并在报告者不要求匿名时给予署名。

## 漏洞披露

公开漏洞前，请为修复与发布预留合理时间。修复可用后，项目可能发布 GitHub Security Advisory，说明受影响版本、影响、缓解方式和报告者署名。

项目欢迎尊重隐私、避免不必要数据访问并遵守本策略的善意安全研究。
